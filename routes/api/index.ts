// import {Router} from "express";
// import authItemsRoute from "./auth-routes";
// import getItemsRoute from "./non-auth-routes"
// export const authRoutes = (app, authMiddleware) => {
//     app.use(authMiddleware, Router().use("/auth-items/items", authItemsRoute) )
// }
// export const nonAuthRoutes = Router().use("/non-auth-items/items", getItemsRoute);


// //apiRouters.use("/items", itemsRouter);



